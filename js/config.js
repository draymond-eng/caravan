/* =============================================================================
   Caravan — Supabase config. Fill with YOUR project's values
   (Supabase → Project Settings → API Keys):
     url     = Project URL, e.g. https://xxxxxxxx.supabase.co
     anonKey = publishable / anon key (safe to commit; RLS is the guard)
   Then run supabase/schema.sql once in the SQL Editor.
   ========================================================================== */
window.CARAVAN_CONFIG = {
  url: "",
  anonKey: "",
};
